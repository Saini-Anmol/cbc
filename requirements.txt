# B2C scheduler — runtime dependencies (engine + DB adapter + Flask API).
# Pinned to the versions verified in the myenv virtualenv.
Flask==3.1.3
gunicorn==23.0.0
SQLAlchemy==2.0.50
PyMySQL==1.2.0
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
openpyxl==3.1.5
