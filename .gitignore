# Data directories (large files)
data/
# Secrets
.env

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Docker bind-mount output (generated plan workbooks)
docker_output/
# Bipartiate graph is checking and having a look for the correct mould assignment work.

# Now, we have implemented the different size CO on the building machine rules as well. But, we got too much reduction in our overall KPIs. But, we have a lot of building machines idle. why we are not able to utilise the machine capacity and how can we improve our KPIs? Provide apporach for this, and root cause is idle building machine. Right? optimizer/
optimizer/

# generated validation reports
feasibility_report_*.xlsx
