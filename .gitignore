# Data directories (large files)
data/

# Secrets
.env

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# We were using the mould data from the db, but that data is wrong, I will provide the latest mould data in terms of SKU and number of moulds for that SKU and in another sheet i will provide the SKUs that uses the common moulds. Will that info be enough of consider info & now we will not consider this db table- SELECT * FROM jkplanningV1.Master_Mapping_Mould_SKU; First confirm me this info and then i will provide the excel file in the input folder with the path. 

# Earlier we were using this machine allocation logic- VMI (6 code) then VMI (7 code) then BJ then Unistage and then Stage2 machine, but this logic was not optimal. You will use this logic- we brainstorm this logic with me, then we will decide is this really a good approach? Main approach now- 1. we needs to decide in which gorup this SKU is eligible to run from the above machine groups- then we will select in which group we should select- selction will be based on this logic- we will select total elgible demand on this machine and demand of the current SKU that we wants to run, so will be get a ratio for this, so select the machine group with the maximum ratio. Now, we enters into a machine gorup and now we have to decide the particular machine as well- so within that machine gorup again we needs to calculate the ration for all the eligible machines in that group and again we have to calculate the ratio in the similar manner and then select the machine with the highest raito, if that machine is not able to fulfill the current requirement then again we have to select another machine with the second max. ratio in that machine group and similarly we will decide the number of machine required to fulfill the curing consumption demand, when the qty is produced then we needs to remove this from the denominator or we should not? I am confused here? - very simple for the RI SKU and when the NRI SKUs got the curing press then we needs to search the above ratio from the available machines [group and then particular machine]. Main aim for this approach- we wants to assign those SKUs first which are very sensitive to machines- means 7301(newirm) machine can only produce this SKU [1225170015012LSTL0], if we assign this SKU to other eligble machines like BJ machines. Then, 7301(newirm) will remain idle for the whole month which is bad, means i must have to produce this SKU on stage1 and then to 7301, if the qty still remaining we have to assign another machines as well accordinly. Any doubt in this approach- there can be multiple edge cases exixts in this approach, provide me those cases- your suggestion to incorporate that in our pipeline and how much KPIs and number we can increase from this process.
# again we have to calculate the ratio for machine group and then inside the machine group as well. 
# We will incorporate building running machines as well, We are deciding building machine based on the requirement of curing, now using the running machine data from the 1st May, now we will directly able to map the building machine required for the press. - with building to curing press mapping. So, for running SKUs our approach will be very simple and we needs to incorporate the above logic for the NRI SKUs only. My understanding is correct? I am missing something- or my approach is perfectly correct? docker_output/
